# Jenkins Shared Library Global Variables

To provide custom DSL to a Jenkinsfile through this shared library, add a file using 
the format `<functionName>.groovy` which can then be called using `<functionName>(...)`
